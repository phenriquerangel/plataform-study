replicaCount: 1

image:
  repository: phenriquernagel/api-questoes
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8000

resources: {}

postgres:
  user: postgres
  password: postgres
  db: questoes

ingress:
  enabled: false
